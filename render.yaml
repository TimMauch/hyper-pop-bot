services:
  - type: worker
    name: fansale-bot
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python fansale_bot.py
